






<li id=<%= "frog_#{frog.id}" %>
<h3>
  <tr>
    <td><%= image_tag frog.user.photo.url(:small) %></td>	
    <td><%= link_to frog.user.name, frog.user %></td>
    <td><%= frog.name %></td>
    
  </tr>
</h3>
</li>
