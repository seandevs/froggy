<h1>Editing frog</h1>

<% form_for(@frog) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @frog %> |
<%= link_to 'Back', frogs_path %>
